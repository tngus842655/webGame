-- 매체가 붙지 않은 광고 기록을 지운다 (20260807000000의 후속)
--
-- 그 마이그레이션 전에 쌓인 행은 테스트 광고(5초 스텁·구글 테스트 단위)와 실제가
-- 섞여 있고, 어느 쪽인지 가릴 단서가 행에 남아 있지 않다. 통계에서는 이미 빼고
-- 있었지만 표에 남겨 둘 쓸모도 없어 지운다.
--
-- 반드시 20260807000000_ad_views_medium.sql을 먼저 돌릴 것. medium 컬럼이 없으면
-- 이 파일은 그냥 실패하고 아무것도 지우지 않는다 — 순서를 틀려도 사고가 나지는 않는다.
-- 앞으로 들어오는 행에는 매체가 항상 붙으므로 여러 번 실행해도 안전하다.
--
-- 되돌릴 수 없다. 지우기 전에 몇 건인지 보고 싶으면:
--   select count(*) from public.ad_views where medium is null;

delete from public.ad_views where medium is null;
