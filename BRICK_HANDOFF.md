# 벽돌깨기 이어서 하기

2026-08-03 작업분 인수인계. **왜 그렇게 했는지**는 [GAME_BALANCE.md](./GAME_BALANCE.md) 3장에
있고, 여기는 **어디서부터 이어서 만지면 되는지**만 적는다.

브랜치 `claude/breakout-analysis-review-ly9ebt` · 커밋 `7f0febe`, `b0fc660`

---

## 1. 지금 상태 한 줄

각도 제한 하나로 눕혀 쏘기 익스플로잇이 죽었다. **목표 넷 중 셋 달성**,
남은 하나는 흔들림(운의 비중)이 너무 큰 것.

| 기준 | 목표 | 지금 |
|---|---|---|
| 전부 게임오버 | 100% | **100/100** ✓ |
| 도달 웨이브 | 25~90 | **26~33** ✓ |
| 한 판 길이 | 3분 이내 | **28~82초** ✓ |
| 배수 (상급/정밀) | 1.3 이하 | 1.49 ✗ (폭탄 빼면 1.09) |
| 흔들림 | 40% 이하 | **103~160%** ✗ |

종전 대비: 눕혀 쏘기가 397웨이브·61~106분·게임오버 1/20 → 33웨이브·82초·게임오버 100/100.

---

## 2. 오늘 바꾼 것

### 게임 규칙

| 항목 | 종전 → 지금 | 위치 |
|---|---|---|
| 조준 각도 제한 | 0.15 (9°) → **0.45 (27°)** | `config.ts` `MIN_AIM_SLOPE` |
| 수평 조준 처리 | 막아서 조준선 삭제 → **최소 각도로 스냅** | `index.ts:65` |
| 블록 | 7열 → **14열** (47×47 정사각, 버퍼 18줄) | `config.ts` `LAYOUT` |
| 밀도 | 35% → **85%** (한 줄 11~13칸) | `config.ts` `WAVE.minBricks/maxBricks` |
| 강화 | 골드·상점 → **벽돌에 심은 공격력 아이템 한 종류** | `state.ts` `rollKind()` |
| 폭탄 벽돌 | 없음 → **둘레 8칸에 제 maxHp만큼, 연쇄** | `index.ts:100` `explode()` |
| 공 | 반지름 10→7, 스텝 14→6, 개수 **5로 고정** | `config.ts` `BALL` |
| 진동 | 벽돌마다 → **턴당 한 번** | `index.ts:133` |

### 걷어낸 것

`RunUpgrades` / `freshUpgrades` / `upgradeCost` / `upgradeLevel` / `tryPurchase` /
`UPGRADES` / `BUTTON_RECTS` / 골드 HUD / 상점 버튼 / i18n `brick.balls` (13개 언어).

`state.run.attackLevel` → `state.attack`, `Brick.item: boolean` → `Brick.kind: BrickKind`.

### 문구

13개 언어 `guides.ts` 의 `brick.how` / `brick.score` 를 골드·공 개수 설명에서
아이템·폭탄 설명으로 교체.

---

## 3. 만질 수 있는 값

전부 `src/games/brick/config.ts` 한 곳에 있다.

```ts
MIN_AIM_SLOPE = 0.45      // 확정. 이걸 낮추면 익스플로잇이 돌아온다
BALL.count    = 5         // 늘리면 턴 시간이 그만큼 길어진다
BALL.step     = 6         // 반지름(7)보다 작아야 벽돌을 안 뚫는다. 건드리지 말 것

WAVE.hpGrowth     = 1      // 잠정. 벽돌 HP = ceil(웨이브 × 이 값)
WAVE.itemChance   = 0.07   // 잠정
WAVE.bombChance   = 0.015  // 잠정
```

**노브 반응** (100판 기준, 배수 = 상급/정밀)

| `bombChance` | 배수 | 비고 |
|---|---|---|
| 0 | 1.09 | 폭탄 없음 |
| 0.015 | **1.49** | 지금 값. 한 판에 4~5번 터짐 |
| 0.03 | 1.81 | 너무 셈 |

`hpGrowth` 를 1 밑으로 내리면 게임이 살 만해지는 대신 배수가 폭발한다
(0.85/아이템0.09 → 배수 17.2, 0.70/0.12 → 999). 내리지 말 것.

---

## 4. 재는 법

```
node tools/brick-sim.mjs 100          정책 다섯을 100판씩
node tools/brick-sim.mjs 40 sweep     아이템 확률 × HP 성장률 격자
```

- `config.ts`·`state.ts` 를 그대로 불러 쓰므로 **상수를 고치면 바로 반영된다.**
- 공 물리와 턴 진행은 `index.ts` 를 옮겨 적은 것이다 —
  **`moveBall` / `collideBricks` / `breakBrick` 을 고치면 시뮬레이터도 같이 고쳐야 한다.**
- 100판에 10~15분 걸린다. 24판은 배수가 1.96~7.49까지 흔들려서 판정에 못 쓴다.
- 집계는 중앙값이다. 상급 정책의 꼬리가 길어 평균은 몇 판에 통째로 끌려간다.

**계단 다섯**: 초보(수직) · 무작위 · 입문(낮은 벽돌) · 정밀(45° 이상, 위치만) · 상급(최소 각도).
배수의 분모는 **정밀**이다. 입문은 예전 표와 이어 보려고 같이 찍을 뿐이다.

---

## 5. 다음에 할 것 (우선순위)

### ① 흔들림 낮추기 — 유일하게 남은 미달 항목

정밀 155~160% / 상급 103~115%. 기준은 40% 이하(GAME_BALANCE.md 1.3).

원인이 짚인다. 아이템이 **벽돌마다 7% 독립 추첨**이라 한 줄에 12칸이어도
`P(아이템 0개) = 0.42`다. 초반에 운이 없으면 공격력이 안 자라 그대로 무너진다.

→ **줄마다 개수를 정해 뽑는 방식**으로 바꾸면 평균은 그대로 두고 분산만 줄일 수 있다.
   (예: 줄마다 84% 확률로 정확히 1개 — 기댓값 0.84로 지금과 같다)
   고칠 곳은 `state.ts` `spawnRow` + `rollKind`.

**성공 기준:** 정밀·상급 흔들림이 60% 밑으로, 웨이브 25~90 유지.

### ② 재미 요소를 더 넣는다면

**조건: 접촉 수에 비례하지 않을 것.** 이게 이번 라운드의 결론이다.

이 게임의 보상은 전부 접촉 수에 비례하고, 접촉을 최대화하는 게 눕혀 쏘기다
(경로 `P = 2h/sinθ`). 그래서 접촉당 이득을 키우거나 경로를 늘리는 건
**무엇이든 눕혀 쏘기 쪽에 더 얹힌다.**

- 포켓(세로 골목) → 배수 1.09 → 1.97. **되돌림**
- 폭탄 3% → 배수 1.81. 1.5%로 낮춰서 넣음
- 아이템 확률 상향 → 격자 탐색에서 배수 폭발

한 턴에 **최대 한 번만** 받을 수 있는 종류여야 한다. 예: 매 턴 표적 한 칸을
지정하고 그걸 맞히면 보상. 접촉이 많다고 더 못 받으니 눕혀 쏘기가 이득을 못 본다.

### ③ 실기 확인이 필요한 것 (내가 못 재는 부분)

- 47px 벽돌에서 **HP 숫자**가 읽히는지 (`h × 0.5`까지 키웠다. 실기 환산 ×0.542)
- **아이템 흰 테두리**와 **폭탄 검은 원**이 구분되는지
- 폭탄 터질 때 손맛. 지금은 `explode` 효과음 + 칸마다 흰 섬광뿐이다
- 밀도 85%에서 화면이 답답하지 않은지

아이콘을 제대로 넣고 싶으면 이미지를 주면 교체한다 (지금은 캔버스 도형뿐).

---

## 6. 안 한 것 / 접은 것

- **공 수명(턴 시간) 상한** — 배수를 더 잡는 수단으로 유력했으나, 정책을 고치고
  다시 재니 배수 1.09라 필요가 없어졌다. 폭탄 확률을 올릴 거면 다시 볼 것
- **강철 벽돌**(안 깨지고 반사만) — 경로를 끊는 세 번째 레버. 게임오버 설계를
  같이 봐야 해서 안 건드렸다
- **다음 턴 한정 버프**(관통·분열) — 상수도 아직 잠정인데 변수를 더 늘리면
  측정이 불가능해진다
- **데일리·스트릭** — GAME_BALANCE.md 4장 권고 3번이고, 대상은 상위 4개
  (수박·블록퍼즐·보석매치·과일2048)다. 벽돌깨기는 순서가 아니다
