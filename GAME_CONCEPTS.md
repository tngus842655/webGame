# 게임 라인업

> 기술 구조는 [DESIGN.md](./DESIGN.md) 참고.

## 운영 계획

- 1차 목표 **30개**까지 게임을 채우고, 이후에도 매주 몇 개씩 추가한다.
  현재 **25개 구현 완료, 5자리 남음** — 채울 후보는 아래 '다음 추가'에 정리해 뒀다.
- 게임끼리 경쟁시킨다: 최근 7일 플레이 시간 기반 인기 순위가 홈 화면 정렬에 반영되어
  있으므로, 성적이 나쁜 게임은 자연스럽게 뒤로 밀린다. 데이터를 보고 개선하거나 뺀다.

### 게임 선정 기준

- **1순위 중독성** — 짧은 보상 주기, "한 판 더" 루프, 랭킹 경쟁
- **2순위 UI 단순성** — 튜토리얼 없이 30초 안에 이해
- 수익 모델(리워드 광고·재화 판매)과 궁합이 좋을 것
- 제외: 방치형, 그래픽 에셋 의존도가 높은 게임, 실시간 멀티(서버 비용)

## 구현 완료 (25)

| # | slug | 게임명 | 장르 · 조작 |
|---|---|---|---|
| 1 | `suika` | 수박 합치기 | 물리 합성 퍼즐 · 좌우 조준 + 탭 |
| 2 | `blockblast` | 블록 블라스트 | 격자 줄 완성 퍼즐 · 드래그 배치 |
| 3 | `brick` | 벽돌깨기 | 블록 브레이커 · 드래그 조준 발사 |
| 4 | `fruit2048` | 과일 2048 | 2048 변형 · 스와이프 |
| 5 | `runner` | 무한 러너 | 장애물 회피 · 원버튼 점프 |
| 6 | `dodge` | 낙하물 회피 | 생존 시간 경쟁 · 좌우 이동 |
| 7 | `survivor` | 서바이버 | 뱀서라이크 · 이동만 조작(자동 공격) |
| 8 | `merge` | 머지 가든 | 머지 보드 · 드래그 앤 드롭 |
| 9 | `match3` | 보석 매치3 | 매치3 퍼즐 · 탭/드래그 스왑 |
| 10 | `tripeaks` | 트라이픽스 솔리테어 | 카드 솔리테어 · 탭 |
| 11 | `nonogram` | 네모로직 | 힌트 논리 퍼즐 · 탭/드래그 |
| 12 | `sudoku` | 스도쿠 데일리 | 날짜별 퍼즐 + 스트릭 · 탭 + 숫자 패드 |
| 13 | `pipes` | 파이프 연결 | 회전 물길 퍼즐 타임어택 · 탭 |
| 14 | `defense` | 타워 디펜스 | 랜덤 소환·합성 웨이브 방어 · 탭/드래그 |
| 15 | `jump` | 점프 챌린지 | 홀드 차지 점프, 최고 높이 경쟁 · 홀드 |
| 16 | `rhythm` | 리듬 탭 | 4레인 낙하 노트 · 탭 |
| 17 | `orbit` | 궤도 슈팅 | 회전 포탑 타이밍 사격 · 탭 |
| 18 | `wordle` | 한글 워들 | 자모 6칸 데일리 단어 · 탭 |
| 19 | `omok` | 오목 | 대 AI 연승 사다리 · 탭 |
| 20 | `deck` | 카드 배틀 | 간소화 덱빌딩 로그라이크 · 탭 |
| 21 | `autochess` | 오토 배틀 | 유닛 구매·합성 자동 전투 · 탭/드래그 |
| 22 | `stack` | 탑 쌓기 | 타이밍 아케이드 · 아무 곳 탭 |
| 23 | `sortgate` | 분류 반장 | 순간판단 분류 · 좌우 스와이프/절반 탭 |
| 24 | `numorder` | 숫자 줄 세우기 | 오름차순 끼워넣기 퍼즐 · 빈 칸 탭 |
| 25 | `flashnum` | 찰나의 숫자 | 순간기억 · 순서대로 탭 |

## 다음 추가 — 후보 10개 (2026-07 선정)

30개까지 **아홉 자리**가 남았다. 아래 열 개는 그 자리를 채우기 위해 뽑은 후보다.
열 개인 이유는 하나를 뺄 여지를 두기 위해서다 — **확신이 높은 순으로 정렬**되어 있고
10번 `bamboo`가 가장 먼저 뺄 후보다.

선정 과정: 여섯 관점(반응속도·공간퍼즐·기억관찰·물리·정밀조작·규칙분류)에서 36개를
만들고, 세 관점(기존 21개와의 중복성 · 기술 제약 하 구현가능성 · 재미와 연령 적합성)으로
교차 심사해 한 곳이라도 거부한 후보를 떨어뜨린 뒤 장르 분포를 보고 골랐다.

### 이 조합을 고른 이유

- 기존 21개에 **기억·관찰·순간판단** 세 카테고리가 통째로 비어 있다 →
  `flashnum`(기억) · `samepic`(관찰) · `sortgate`(판단 전환)로 먼저 메운다.
- 퍼즐은 이미 다섯 개(논리 3 + 블록 2)다. 개수를 늘리는 대신 **판단축이 겹치지 않는**
  셋만 골랐다 — 끼워 넣기(`numorder`) · 벽으로 역산(`iceslide`) · 자원 배정(`marblejar`).
- **프리핸드 드로잉 입력이 21개에 전무**하다 → `comet`(직선 긋기) · `paperboat`(다중 경로)
  두 갈래로 넣는다.
- 아케이드는 `stack` 하나로 충분해서 같은 계열인 `bamboo`를 맨 끝에 뒀다.
- 난이도는 쉬움 6 · 보통 4 · 어려움 0. 물리 튜닝이나 솔버 개발에 발목 잡히는 후보는
  전부 걸렀다(아래 '떨어뜨린 후보' 참고).

| # | slug | 게임명 | 장르 | 조작 | 구현 |
|---|---|---|---|---|---|
| ~~1~~ | ~~`stack`~~ | ~~탑 쌓기~~ | 타이밍 아케이드 | 아무 곳 탭 1회 | **완료** |
| ~~2~~ | ~~`sortgate`~~ | ~~분류 반장~~ | 순간판단 | 좌우 스와이프(또는 좌우 절반 탭) | **완료** |
| ~~3~~ | ~~`numorder`~~ | ~~숫자 줄 세우기~~ | 배치 퍼즐 | 빈 칸 탭 | **완료** |
| ~~4~~ | ~~`flashnum`~~ | ~~찰나의 숫자~~ | 순간기억 | 순서대로 탭 | **완료** |
| 5 | `paperboat` | 종이배 뱃길 | 실시간 경로관리 | 자유 곡선 드래그 | 보통 |
| 6 | `samepic` | 같은 그림 찾기 | 순간관찰 | 탭 | 보통 |
| 7 | `comet` | 별똥별 잇기 | 궤적 액션 | 프리핸드 긋기 | 보통 |
| 8 | `iceslide` | 펭귄 미끄럼 | 턴제 이동퍼즐 | 상하좌우 스와이프 | 보통 |
| 9 | `marblejar` | 구슬 정리함 | 분류 스택 | 통 탭 | 쉬움 |
| 10 | `bamboo` | 대나무 타기 | 속도전 액션 | 좌우 절반 탭 | 쉬움 |

---

### 1. `stack` 탑 쌓기 — 타이밍 아케이드 · 쉬움

- **조작**: 화면 아무 곳이나 탭 1회.
- **루프**: 좌우로 왕복하는 블록을 탭해 탑 꼭대기에 얹는다. 어긋난 만큼 블록 폭이
  깎여 나가고, 다음 블록은 그 좁아진 폭으로 시작한다. 폭이 0이 되면 끝.
- **종료·난이도**: 층이 오를수록 블록 왕복 속도가 빨라진다. 실패가 즉사가 아니라
  **수명이 줄어드는 누적 페널티**인 것이 핵심 — 폭 자체가 게이지 역할을 해서
  "앞으로 두 번 더 실수하면 끝"이 화면에 그대로 보인다.
- **광고 접점**: 무너진 직후 '폭 절반 복구'. 무엇을 사는지 설명이 필요 없다.
- **드로잉**: 직사각형만. 층마다 색상(hue)을 조금씩 돌려 그라데이션 탑을 만든다.
- **차별점**: 15 `jump`는 홀드로 힘을 모아 중력에 맡기지만 여기엔 중력도 캐릭터도 없다.
- **약점**: 조작 자체는 원버튼 타이밍이라 5 `runner`·17 `orbit`과 손가락 움직임이 같다.
- **비고**: 상태가 (폭, 층수, 속도) 셋뿐이라 이 목록에서 구현 확실성이 가장 높다.

### 2. `sortgate` 분류 반장 — 순간판단 · 쉬움

- **조작**: 가운데 도형을 좌우로 스와이프(또는 화면 좌우 절반 탭).
- **루프**: 위쪽 두 상자에 붙은 기준(색/모양/크기)을 보고 도형을 맞는 쪽으로 민다.
  **몇 개마다 기준이 통째로 갈아 끼워진다** — 손은 이미 아까 기준으로 움직이고 있어
  어이없이 틀린다. 그 순간이 웃기면서도 명백히 내 탓이라 재도전이 즉각적이다.
- **종료·난이도**: 목숨 3개. 도형 낙하 속도 + 기준 교체 주기를 함께 조인다.
- **광고 접점**: 목숨이 다하면 '목숨 하나 더'. **재개 직전 현재 기준을 3초간 크게**
  보여 줘야 억울함이 남지 않는다.
- **드로잉**: 원·삼각형·사각형 × 3색 × 2크기. 상자 기준은 도형 아이콘으로만 표시.
- **차별점**: '규칙 전환 자체가 난이도'인 게임은 기존 21개에 없다. 16 `rhythm`처럼
  어디를 누를지 화면이 미리 알려주지도 않는다.
- **약점**: 기준 교체 연출이 조금이라도 애매하면 '함정'으로 읽힌다 — 예고 연출
  (기준 상자가 번쩍이고 카운트다운)에 공을 들여야 한다.
- **비고**: **글자를 한 자도 안 써서 13개 언어 번역 비용이 0이다.**

### 3. `numorder` 숫자 줄 세우기 — 배치 퍼즐 · 쉬움

- **조작**: 빈 칸 탭.
- **루프**: 1~999 숫자 카드가 한 장씩 나온다. 세로로 늘어선 칸(예: 20칸)에
  **위에서 아래로 커지도록** 끼워 넣는다. 넣을 수 있는 칸이 없으면 끝.
- **종료·난이도**: 칸 수를 늘리는 대신 판마다 칸 수를 고정하고, 살아남은 칸 수로 점수.
  판단이 "여기 넣으면 나중에 막히지 않을까" 하나뿐이라 규칙 설명이 필요 없다.
- **광고 접점**: '막힘의 원인이 된 칸 비우기'. 플레이어가 이미 "그 칸만 아니었으면"
  이라고 생각하는 순간에 정확히 걸린다.
- **드로잉**: 둥근 사각형 + 숫자 텍스트. 넣을 수 있는 칸은 테두리 강조.
- **차별점**: 10 `tripeaks`는 깔린 카드를 걷어내는 제거형, 이건 새 카드를 끼워 넣는
  배치형. 2 `blockblast`와 달리 모양이 없이 대소 관계만 본다.
- **약점**: 세 자리 수 비교라 미취학 아동에게는 부담이다(초등 이상 기준).
- **비고**: 죽었을 때 몇 수 전에 어디를 잘못 넣었는지 정확히 기억나기 때문에
  재도전 동기가 이 목록에서 가장 선명하다.

### 4. `flashnum` 찰나의 숫자 — 순간기억 · 쉬움

- **조작**: 순서대로 탭.
- **루프**: 숫자가 적힌 물방울이 1초 남짓 떠 있다가, **첫 물방울을 탭하는 순간
  나머지 숫자가 전부 지워진다.** 잔상만으로 순서대로 눌러 터뜨린다.
- **종료·난이도**: 하트 3개. 개수(4→9개)와 노출 시간(1.2초→0.4초) 두 축으로 올린다.
  한 레벨이 10초라 재도전 부담이 사실상 0이다.
- **광고 접점**: 하트가 0이 된 순간 '이 레벨부터 다시'. 최고 기록 직전에서 끊긴
  판을 정확히 겨냥한다.
- **드로잉**: 원 + 숫자. 지워진 뒤에는 반투명 원만 남긴다.
- **차별점**: 순간기억 장르가 기존 21개에 전무하다. 12 `sudoku`가 '보이는 정보로
  추론'하는 정적 퍼즐인 것과 정반대로 이건 '사라진 정보를 재생'한다.
- **약점**: 같은 기억 계열인 '징검다리 건너기(stones)'와 상호작용이 거의 동일하므로
  **둘 중 하나만** — 두 축으로 난이도를 올릴 수 있는 이쪽을 택했다.
- **비고**: 침팬지 테스트 계열로 바이럴성이 이미 검증된 형식이다.

### 5. `paperboat` 종이배 뱃길 — 실시간 경로관리 · 보통

- **조작**: 배에서 부두까지 손가락으로 자유 곡선을 긋는다.
- **루프**: 흘러 들어오는 종이배를 같은 색 부두까지 항로를 그어 보낸다. 배가 늘어날수록
  이미 그어둔 항로끼리 교차점이 생겨, **대여섯 척을 번갈아 손질하는 멀티태스킹이
  실력이 된다.** 배끼리 충돌하면 끝.
- **종료·난이도**: 배 생성 주기를 조이고 색 종류를 늘린다.
- **광고 접점**: 충돌 직후 '그 두 척만 치우기'. 나머지 배와 콤보를 그대로 살려 주므로
  손실 복구가 눈에 보인다.
- **드로잉**: 삼각형 배 + 원형 부두 + 항로는 폴리라인. 색으로만 짝을 표시.
- **차별점**: **내가 조종하는 아바타가 아예 없다** — 6 `dodge`·17 `orbit`과 조작
  감각이 완전히 다르다. 13 `pipes`가 격자 위 정적 퍼즐인 것과 달리 격자 없는 자유
  곡선을 실시간으로 나눠 그린다.
- **약점**: 움직이는 작은 배를 정확히 집어야 해서 **히트 반경을 40px 이상 넉넉히**
  잡는 입력 보정이 필수. 첫 1분 진입 곡선을 아주 완만하게 잡지 않으면 저연령이
  튕겨 나간다.

### 6. `samepic` 같은 그림 찾기 — 순간관찰 · 보통

- **조작**: 탭.
- **루프**: 위아래 두 원판에 **공통으로 들어 있는 그림 하나**를 찾아 탭한다.
  한 라운드가 2~3초라 콤보 리듬이 이 목록에서 가장 빠르다. 생각보다 손이 먼저
  움직이다가 한 번 막히는 순간이 억울해서 그대로 다음 판으로 이어진다.
- **종료·난이도**: 20초 게이지. 맞히면 조금 회복, 심볼 개수를 6→8개로 늘린다.
- **광고 접점**: 게이지가 0이 된 순간 '+10초, 콤보 절반 유지'. 콤보가 한창일 때
  끊기는 구조와 정확히 맞물린다.
- **드로잉**: 원판 두 개 + 심볼. **심볼 20종을 `Path2D`로 직접 그려야 한다.**
- **차별점**: 9 `match3`와 소재만 비슷할 뿐 격자·스왑·연쇄 제거가 하나도 없는
  순수 탐색 게임이다.
- **약점**: 이 목록에서 **유일하게 수작업 콘텐츠가 있다**(기존 `tripeaks/cardArt.ts`
  수준). 720 폭에 원판 두 개를 넣으면 심볼 8개일 때 겹침 없는 배치와 터치 타깃
  확보가 빡빡하다.
- **비고**: 글자 없이 규칙이 전달돼 전 연령 적합성이 최상급이다.

### 7. `comet` 별똥별 잇기 — 궤적 액션 · 보통

- **조작**: 프리핸드로 한 획 긋기.
- **루프**: 포물선으로 떨어지는 별똥별들을 한 획으로 이어 터뜨린다. **한 번에 몇 개를
  이었느냐로 점수가 크게 뛴다.** 안전하게 하나씩 지우면 점수가 안 오르고, 세 개
  겹치는 순간을 기다리면 놓칠 위험이 커지는 욕심 구조.
- **종료·난이도**: 놓친 개수 3개면 끝. 사출 빈도와 속도를 올린다.
- **광고 접점**: 놓친 개수를 0으로 되돌리기. 난이도가 이미 오른 고점 구간에서
  기댓값이 크다.
- **드로잉**: 원 + 꼬리(선분 페이드) + 그은 궤적은 폴리라인.
- **차별점**: 21개 중 '어떻게 긋느냐'가 곧 점수인 게임은 없다 — 4 `fruit2048`은
  4방향 입력이고 9 `match3`는 두 칸 스왑이다.
- **약점**: 사출 각도가 무작위면 큰 궤적이 성립 안 하는 판이 나와 **운으로 읽힌다**
  → 뭉쳐 올라오는 패턴을 의도적으로 섞을 것. 점수는 제곱이 아니라 **계단식 배수로
  캡**을 걸어야 분당 2,000점 안에 든다.
- **비고**: 폐곡선을 그리는 '물고기 그물(netcatch)'과 판단이 사실상 같아 하나만 택했다.

### 8. `iceslide` 펭귄 미끄럼 — 턴제 이동퍼즐 · 보통

- **조작**: 상하좌우 스와이프.
- **루프**: 스와이프하면 펭귄이 벽이나 바위에 부딪힐 때까지 **한 번에 미끄러진다.**
  중간에 멈출 수 없으니 '어디에 부딪혀 멈출까'를 벽으로 역산해야 하고, 계산대로
  딱 별 위에 서는 순간이 짧고 시원하다.
- **종료·난이도**: 스와이프 횟수 제한. 판을 깨면 다음 판, 횟수를 다 쓰면 끝.
  바위 배치와 목표 개수로 난이도를 올린다.
- **광고 접점**: 게임오버에서 '+5회'. 판 중간 되돌리기는 1회 무료 후 광고.
- **드로잉**: 격자 + 원형 펭귄 + 사각 바위 + 별. 미끄러지는 보간 애니메이션 필수.
- **차별점**: 4 `fruit2048`과 입력만 같을 뿐 **보드가 밀리지도 합쳐지지도 않고
  움직이는 대상이 하나뿐**이다. 실시간 회피와 달리 손을 떼고 고민해도 되는 완전 턴제.
- **약점**: 얼음 미끄럼은 본질적으로 **시행착오형**인데 스와이프 여유가 빠듯하면
  탐색을 벌하는 꼴이라 억울한 종료가 잦다 → **되돌리기 무료 횟수를 넉넉히** 줄 것.
- **비고**: 같은 격자 이동 계열인 '주차장 탈출(unblock)'은 매 스테이지 BFS 솔버가
  필수라 알고리즘 부담이 커서 뺐다.

### 9. `marblejar` 구슬 정리함 — 분류 스택 · 쉬움

- **조작**: 통 탭.
- **루프**: 무작위 색 구슬이 한 알씩 나오고, **통 네 개 중 어디에 넣을지만** 고른다.
  통에는 정해진 색이 없어서 '어느 통을 어느 색 전용으로 쓸지' 배정하는 결정 하나가
  게임의 전부다. 같은 색이 통을 가득 채우면 비워지고 점수. 통이 다 차면 끝.
- **종료·난이도**: **색 종류가 통 수보다 많다**는 규칙이 붕괴를 수학적으로 예고하므로,
  망가진 통을 언제 포기하고 언제 되살릴지 저울질이 계속된다.
- **광고 접점**: '가장 꽉 찬 통 하나 비우기'. 보상이 즉시 눈에 보인다.
- **드로잉**: 원기둥(사각형) 통 + 원 구슬. 색만으로 구분.
- **차별점**: 1 `suika`와 달리 물리도 조준도 없고, 8 `merge`와 달리 등급이 없다.
- **약점**: **4색·4통 초반은 결정할 게 없어 지루하므로 5색부터** 시작할 것.
- **비고**: 색 분류 소재라 '물감 병 정리(watersort)'와 동시 채택하면 소재가 겹쳐
  보인다. watersort는 완전 막힘이 거의 안 생겨 런이 끝나지 않는 문제가 있어 제외했다.

### 10. `bamboo` 대나무 타기 — 속도전 액션 · 쉬움 — **가장 먼저 뺄 후보**

- **조작**: 화면 좌우 절반 중 가지가 없는 쪽 탭.
- **루프**: 탭할 때마다 한 마디씩 오르고, **시간 막대는 쉬는 동안 계속 줄어든다.**
  '천천히 안전하게'라는 선택지를 시간 막대가 원천 차단해 연타 리듬을 강제한다.
- **종료·난이도**: 가지에 부딪히거나 시간이 다하면 끝. 시간 감소 속도를 올린다.
- **광고 접점**: 부딪힌 마디 바로 아래에서 시간 막대를 채워 재개.
- **드로잉**: 줄기 사각형 + 가지 베지어 3가닥 + 캐릭터 좌우 반전. **구현이 가장 가볍다.**
- **약점**: 실력 상한이 결국 손가락 속도라 **판단의 깊이가 얕다.** 5 `runner`·
  15 `jump`·17 `orbit`에 이어 `stack`까지 있는 상태에서 원버튼 액션이 하나 더 쌓이는
  **카테고리 포화**가 명확하다 — 9개로 줄인다면 이것부터 뺀다.
- **대체 후보**: 뺀 자리를 굳이 채운다면 '제기차기(jegi)'나 '주차장 탈출(unblock)'.

### 떨어뜨린 후보 (다시 제안하지 말 것)

교차 심사에서 한 곳 이상이 거부한 것들. 이유를 남겨 같은 아이디어가 되돌아오는 것을 막는다.

- **종이 접기(fold)**: 칸마다 겹친 층 스택을 관리하고 임의의 격자선을 축으로 영역을
  반사·역전시키는 기하 엔진이 필요해 게임당 150~400줄 규모를 크게 벗어난다. 드래그
  한 번으로 '어느 조각을 어느 축으로' 접을지 판정하는 입력도 근본적으로 모호하다.
- **스티커 앨범(sticker)**: 화면에 게임 상태가 하나도 없어 위/아래 스와이프 판정만
  반복된다. 그림 재인 기억은 인간이 원래 아주 잘해서 초반이 지루하고, 색만 다른
  스티커를 섞는 순간 억울한 오답이 터지는 계단식 난이도가 된다.
- **방울 튕기기(bounce)**: 반사면 각도를 정하는 판단이 3 `brick`의 조준과 사실상
  같고 목표·실패 조건까지 동일하다.
- **물감 병 정리(watersort)**: 완전 막힘이 거의 생기지 않아 런이 끝나지 않는다.
- **주차장 탈출(unblock)**: 매 스테이지 BFS 솔버가 필수라 알고리즘 부담이 크다.
  (다만 `bamboo` 대체 후보로는 살아 있다.)

### 착수 전 읽을 것

새 세션에서 위 후보를 구현할 때는 [DESIGN.md](./DESIGN.md)의 **4장 게임 플러그인
규격**과 '새 게임 추가 체크리스트'를 먼저 볼 것. 요약하면 게임 하나를 추가할 때
건드리는 곳은 다섯 군데다.

1. `src/games/<slug>/` — `defineGame(create)`로 `GameModule` 구현.
   `createGameShell()`(rAF 루프·백그라운드 일시정지·정리)과 `CanvasStage`(720×1280
   레터박스·DPR) 위에 얹는다. 상태는 `state.ts`, 그리기는 `renderer.ts`,
   밸런스 상수는 `config.ts`로 나누는 것이 기존 게임들의 관례다.
2. `src/games/registry.ts` — 항목 한 줄 추가
3. `src/shared/i18n.ts` — `game.<slug>` 제목을 **13개 언어** 전부
   (게임 내부 문구도 마찬가지. `en`이 타입 원본이라 키가 없으면 `vue-tsc`가 잡는다)
4. `src/shared/guides.ts` — 목표/조작/점수 세 줄을 **13개 언어** 전부
5. `src/shared/GameIcon.vue` — slug별 벡터 아이콘

공통 장치도 재사용한다. 게임오버 오버레이는 `src/games/overlay.ts`(광고 이어하기
버튼 키를 `adLabelKey`로 넘긴다), 클리어 시 점수 2배 제안은
`src/games/clearBonus.ts`, 최고 기록 표시는 `ctx.getBestScore()`.

점수 상수는 **분당 500~2,000점** 범위에 들어오게 잡는다(아래 '점수 단위 기준').

**i18n 키 접두어** — 게임 내부 문구는 게임마다 짧은 접두어를 쓴다(`sv.`=서바이버,
`dk.`=카드 배틀 …). 같은 언어 블록 안에서 키가 겹치면 **뒤엣것이 조용히 덮어쓰고
타입 검사도 통과**하므로 접두어를 반드시 비워 있는 것으로 고를 것. 위 후보 열 개는
아래 접두어가 비어 있는 것을 확인해 뒀다.

| slug | 접두어 | slug | 접두어 |
|---|---|---|---|
| `stack` | `st.` | `samepic` | `sp.` |
| `sortgate` | `sg.` | `comet` | `cm.` |
| `numorder` | `nm.` | `iceslide` | `ic.` |
| `flashnum` | `fn.` | `marblejar` | `mj.` |
| `paperboat` | `pb.` | `bamboo` | `bm.` |

`numorder`에 `no.`를 쓰면 네모로직과 충돌한다 — 그래서 `nm.`이다.

## 뺀 게임

한때 30개를 채웠지만 아홉을 뺐다.

- `golf`(미니 골프): 조작감이 당구에 가까워 골프답지 않았다
- `focus`(집중 나무): 게임이라기보다 타이머였다
- `roulette`(룰렛 데일리): 운으로만 점수가 정해지고 날짜별 누적 기록이 있어야 성립해
  판마다 초기화하는 방향과 맞지 않았다
- `stock`(모의 주식) · `estate`(부동산 타이쿤): 투기·사행성 소재
- `tuber`(유튜버 시뮬) · `mahjong`(마작 솔리테어): 어린 이용자가 이해하기 어렵거나
  이름이 도박을 연상시킨다
- `reigns`(국가 운영): 지표 관리가 게임이라기보다 통치 시뮬레이션에 가까웠고
  정치·경제 개념 없이는 선택의 의미를 알 수 없었다
- `store`(편의점 운영): 발주·재고·평판을 동시에 굴려야 해 어린 이용자에게 어려웠다

### 점수 단위 기준

게임마다 순위표가 따로 있어 기능상 의미는 없지만, 통일성을 위해 **분당 500~2,000점**
범위에 맞춘다. 새 게임을 만들 때도 이 범위 안에 들어오게 점수 상수를 잡는다.
(2026-07 기준 측정값은 21개 중 20개가 이 범위 안에 있다.)

예외 — **점프 챌린지**는 점수가 곧 도달 높이(m)라 배율을 곱하면 의미가 깨진다.
`447m = 447점`을 유지한다.

### 게임 소재 기준

전 연령이 30초 안에 이해할 수 있어야 한다. 투기·도박을 소재로 하거나 그렇게 보이는
이름은 쓰지 않는다.
이후 추가는 매주 인기 데이터(최근 7일 플레이 시간)를 보고 잘되는 장르 위주로 선정한다.

## 보류

실시간 멀티 계열(io 게임, 퀴즈 배틀, 그림 맞추기, 땅따먹기)은 중독성은 높지만
동시접속 확보와 서버 비용 문제로 보류. 트래픽이 충분해진 뒤 재검토한다.
