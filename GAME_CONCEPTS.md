# 게임 라인업

> 기술 구조는 [DESIGN.md](./DESIGN.md) 참고.

## 운영 계획

- 1차 목표 **30개**까지 게임을 채우고, 이후에도 매주 몇 개씩 추가한다.
  현재 **31개 구현 완료** — 목표를 한 개 넘겼다. 어느 하나를 미리 덜어내지 않은 것은
  인기 데이터가 정하게 하려는 것으로, 30위 밖으로 밀린 게임을 휴지통으로 내리는
  절차는 DESIGN.md '휴지통' 항목에 있다.
- 게임끼리 경쟁시킨다: 최근 7일 플레이 시간 기반 인기 순위가 홈 화면 정렬에 반영되어
  있으므로, 성적이 나쁜 게임은 자연스럽게 뒤로 밀린다. 데이터를 보고 개선하거나 뺀다.

### 게임 선정 기준

- **1순위 중독성** — 짧은 보상 주기, "한 판 더" 루프, 랭킹 경쟁
- **2순위 UI 단순성** — 튜토리얼 없이 30초 안에 이해
- 수익 모델(리워드 광고·재화 판매)과 궁합이 좋을 것
- 제외: 방치형, 그래픽 에셋 의존도가 높은 게임, 실시간 멀티(서버 비용)

## 구현 완료 (31)

| # | slug | 게임명 | 장르 · 조작 |
|---|---|---|---|
| 1 | `suika` | 수박 합치기 | 물리 합성 퍼즐 · 좌우 조준 + 탭 |
| 2 | `blockblast` | 블록 퍼즐 | 격자 줄 완성 퍼즐 · 드래그 배치 |
| 3 | `brick` | 벽돌깨기 | 블록 브레이커 · 드래그 조준 발사 |
| 4 | `fruit2048` | 과일 2048 | 2048 변형 · 스와이프 |
| 5 | `runner` | 무한 러너 | 장애물 회피 · 원버튼 점프 |
| 6 | `dodge` | 낙하물 회피 | 생존 시간 경쟁 · 좌우 이동 |
| 7 | `survivor` | 서바이버 | 뱀서라이크 · 이동만 조작(자동 공격) |
| 8 | `merge` | 머지 가든 | 머지 보드 · 드래그 앤 드롭 |
| 9 | `match3` | 보석 매치3 | 매치3 퍼즐 · 탭/드래그 스왑 |
| 10 | `tripeaks` | 솔리테어 | 카드 솔리테어 · 탭 |
| 11 | `nonogram` | 네모로직 | 힌트 논리 퍼즐 · 탭/드래그 |
| 12 | `sudoku` | 스도쿠 | 날짜별 퍼즐 + 스트릭 · 탭 + 숫자 패드 |
| 13 | `pipes` | 파이프 연결 | 회전 물길 퍼즐 타임어택 · 탭 |
| 14 | `defense` | 타워 디펜스 | 랜덤 소환·합성 웨이브 방어 · 탭/드래그 |
| 15 | `jump` | 점프 챌린지 | 홀드 차지 점프, 최고 높이 경쟁 · 홀드 |
| 16 | `rhythm` | 리듬 탭 | 4레인 낙하 노트 · 탭 |
| 17 | `orbit` | 궤도 슈팅 | 회전 포탑 타이밍 사격 · 탭 |
| 18 | `wordle` | 한글 워들 | 자모 6칸 데일리 단어 · 탭 |
| 19 | `omok` | 오목 | 대 AI 연승 사다리 · 탭 |
| 20 | `deck` | 카드 배틀 | 간소화 덱빌딩 로그라이크 · 탭 |
| 21 | `autochess` | 오토 배틀 | 유닛 구매·합성 자동 전투 · 탭/드래그 |
| 22 | `stack` | 탑 쌓기 | 타이밍 아케이드 · 아무 곳 탭 |
| 23 | `sortgate` | 분류 반장 | 순간판단 분류 · 좌우 스와이프/절반 탭 |
| 24 | `numorder` | 숫자 정렬 | 오름차순 끼워넣기 퍼즐 · 빈 칸 탭 |
| 25 | `flashnum` | 찰나의 숫자 | 순간기억 · 순서대로 탭 |
| 26 | `paperboat` | 종이배 뱃길 | 실시간 경로관리 · 자유 곡선 드래그 |
| 27 | `samepic` | 그림 찾기 | 순간관찰 · 탭 |
| 28 | `comet` | 별똥별 잇기 | 궤적 액션 · 프리핸드 긋기 |
| 29 | `iceslide` | 펭귄 미끄럼 | 턴제 이동퍼즐 · 상하좌우 스와이프 |
| 30 | `marblejar` | 구슬 정리함 | 분류 스택 · 통 탭 |
| 31 | `bamboo` | 대나무 타기 | 속도전 액션 · 좌우 절반 탭 |

## 2026-07 추가분 열 개 (구현 완료)

21개에 열 개를 더해 31개가 됐다. 후보를 열 개 뽑은 이유가 "하나를 뺄 여지를 두기
위해서"였는데 결국 열 개를 다 넣었다 — 선정 당시부터 10번 `bamboo`가 가장 먼저 뺄
후보였고(원버튼 액션 포화), 뺄지 말지는 인기 데이터가 정한다.

선정 과정: 여섯 관점(반응속도·공간퍼즐·기억관찰·물리·정밀조작·규칙분류)에서 36개를
만들고, 세 관점(기존 21개와의 중복성 · 기술 제약 하 구현가능성 · 재미와 연령 적합성)으로
교차 심사해 한 곳이라도 거부한 후보를 떨어뜨린 뒤 장르 분포를 보고 골랐다.

### 이 조합을 고른 이유

- 기존 21개에 **기억·관찰·순간판단** 세 카테고리가 통째로 비어 있었다 →
  `flashnum`(기억) · `samepic`(관찰) · `sortgate`(판단 전환)로 메웠다.
- 퍼즐은 이미 다섯 개(논리 3 + 블록 2)였다. 개수를 늘리는 대신 **판단축이 겹치지 않는**
  셋만 골랐다 — 끼워 넣기(`numorder`) · 벽으로 역산(`iceslide`) · 자원 배정(`marblejar`).
- **프리핸드 드로잉 입력이 21개에 전무**했다 → `comet`(한 획 긋기) · `paperboat`(다중 경로)
  두 갈래로 넣었다.
- 아케이드는 `stack` 하나로 충분해서 같은 계열인 `bamboo`를 맨 끝에 뒀다.
- 물리 튜닝이나 솔버 개발에 발목 잡히는 후보는 전부 걸렀다(아래 '떨어뜨린 후보' 참고).

### 구현하면서 정한 것

| # | slug | 게임명 | 조작 | 밸런스·구현 요점 |
|---|---|---|---|---|
| 1 | `stack` | 탑 쌓기 | 아무 곳 탭 | 폭 300px 시작, 정확히 맞추면 +6px 복구 · 층마다 hue 11도 |
| 2 | `sortgate` | 분류 반장 | 좌우 스와이프/절반 탭 | 기준을 '그 상자에 들어갈 예시 셋'으로 그려 게임 내 글자 0 · 교체 1.4초 예고 |
| 3 | `numorder` | 숫자 정렬 | 빈 칸 탭 | 16칸, 뒤 칸일수록 60→240점 · 다 채우면 +300점과 새 판 |
| 4 | `flashnum` | 찰나의 숫자 | 순서대로 탭 | 개수 4→9개, 노출 1.5→0.45초 · 틀리면 1.2초간 정답 공개 |
| 5 | `paperboat` | 종이배 뱃길 | 자유 곡선 드래그 | 배 집는 반경 48px · 항로 없는 배는 못 안에서 반사 · 최대 9척 |
| 6 | `samepic` | 그림 찾기 | 탭 | 심볼 20종 직접 드로잉 · 6→8개 · 20초 게이지 |
| 7 | `comet` | 별똥별 잇기 | 프리핸드 긋기 | 계단식 배수 최대 3배 · 한 획 700px 상한 · 뭉쳐 오는 무리를 의도적으로 섞음 |
| 8 | `iceslide` | 펭귄 미끄럼 | 상하좌우 스와이프 | 판마다 BFS 최소해 + 4수 지급 · 공짜 되돌리기 3회 |
| 9 | `marblejar` | 구슬 정리함 | 통 탭 | 4통 × 4칸 · 5색 시작, 비운 통 3회마다 +1색(최대 7색) |
| 10 | `bamboo` | 대나무 타기 | 좌우 절반 탭 | 시간 막대 5초, 한 마디당 +0.34초 · 감소 속도만 올린다 |

애초 제안은 '1회 무료 후 광고'였지만 `iceslide`의 되돌리기는 **판마다 3회 무료**로 했다
(다 쓰면 광고 되돌리기). 얼음 미끄럼은 본질적으로 시행착오형이라 탐색을 벌하면 억울한
종료가 잦다 — 선정 단계에서 이미 약점으로 적어 둔 것을 그대로 따랐다.

난수로 판을 만드는 셋은 시뮬레이션으로 확인한 뒤 상수를 정했다.

- `numorder`: 2만 판에서 평균 8칸·816점, 광고 '막힌 칸 비우기' 실패 0회
- `samepic`: 6만 판에서 심볼 겹침 0판(최소 간격 12px 유지)
- `iceslide`: 2만 판을 BFS로 풀어 풀 수 없는 판 0개, 지급 수가 최소해보다 항상 4수 이상 여유
- `marblejar`: 2만 판에서 한 판 약 30알·비운 통 3~4회

### 떨어뜨린 후보 (다시 제안하지 말 것)

교차 심사에서 한 곳 이상이 거부한 것들. 이유를 남겨 같은 아이디어가 되돌아오는 것을 막는다.

- **종이 접기(fold)**: 칸마다 겹친 층 스택을 관리하고 임의의 격자선을 축으로 영역을
  반사·역전시키는 기하 엔진이 필요해 게임당 150~400줄 규모를 크게 벗어난다. 드래그
  한 번으로 '어느 조각을 어느 축으로' 접을지 판정하는 입력도 근본적으로 모호하다.
- **스티커 앨범(sticker)**: 화면에 게임 상태가 하나도 없어 위/아래 스와이프 판정만
  반복된다. 그림 재인 기억은 인간이 원래 아주 잘해서 초반이 지루하고, 색만 다른
  스티커를 섞는 순간 억울한 오답이 터지는 계단식 난이도가 된다.
- **방울 튕기기(bounce)**: 반사면 각도를 정하는 판단이 3 `brick`의 조준과 사실상
  같고 목표·실패 조건까지 동일하다.
- **물감 병 정리(watersort)**: 완전 막힘이 거의 생기지 않아 런이 끝나지 않는다.
- **주차장 탈출(unblock)**: 매 스테이지 BFS 솔버가 필수라 알고리즘 부담이 크다.
  (다만 `bamboo` 대체 후보로는 살아 있다.)

### 착수 전 읽을 것

새 게임을 구현할 때는 [DESIGN.md](./DESIGN.md)의 **4장 게임 플러그인 규격**과
'새 게임 추가 체크리스트'를 먼저 볼 것 — 건드릴 파일과 순서는 거기 있다.
여기서는 그 목록에 없는 것만 적는다.

게임 본체는 `defineGame(create)`로 `GameModule`을 구현하고, `createGameShell()`
(rAF 루프·백그라운드 일시정지·정리)과 `CanvasStage`(720×1280 레터박스·DPR) 위에 얹는다.

공통 장치도 재사용한다. 게임오버 오버레이는 `src/games/overlay.ts`(광고 이어하기
버튼 키를 `adLabelKey`로 넘긴다), 클리어 시 점수 2배 제안은
`src/games/clearBonus.ts`, 최고 기록 표시는 `ctx.getBestScore()`.

**광고 버튼은 반드시 `ctx.isRewardAdReady()`로 가린다**(`overlay.show`의 `canContinue`
인자). 매체를 안 붙인 기본값이 `NoAdProvider`(항상 false)라, 가리지 않으면 눌러도
아무 일이 없는 버튼이 남는다. 개발 중에 보상 흐름을 보려면 `VITE_AD_STUB=on`으로
5초 가짜 광고를 켠다.

**광고 자리 이름**(`ctx.showRewardAd(placement)`의 인자)은 `<slug>-<동작>` 한 꼴로만
짓는다 — `omok-undo`, `iceslide-moves`처럼. 이 문자열이 그대로 `ad_views`에 쌓여
`/stats/ads`의 자리별 집계 축이 되므로, 꼴을 벗어나면 그 게임만 집계에서 겉돈다.

점수 상수는 **분당 500~2,000점** 범위에 들어오게 잡는다(아래 '점수 단위 기준').

**i18n 키 접두어** — 게임 내부 문구는 게임마다 짧은 접두어를 쓴다(`sv.`=서바이버,
`dk.`=카드 배틀 …). 접두어는 반드시 비워 있는 것으로 고를 것 — 키를 통째로 겹쳐 쓰면
`vue-tsc`가 TS1117로 잡아 주지만, 남의 접두어를 나눠 쓰면 그 키가 어느 게임 문구인지
읽어서는 알 수 없게 된다. 2026-07 추가분이
쓴 접두어는 아래와 같다 — 새 게임은 이것들까지 피해야 한다.

| slug | 접두어 | slug | 접두어 |
|---|---|---|---|
| `stack` | `st.` | `samepic` | `sp.` |
| `sortgate` | `sg.` | `comet` | `cm.` |
| `numorder` | `nm.` | `iceslide` | `ic.` |
| `flashnum` | `fn.` | `marblejar` | `mj.` |
| `paperboat` | `pb.` | `bamboo` | `bm.` |

`numorder`에 `no.`를 쓰면 네모로직과 충돌한다 — 그래서 `nm.`이다.

## 뺀 게임

한때 30개를 채웠지만 아홉을 뺐다.

- `golf`(미니 골프): 조작감이 당구에 가까워 골프답지 않았다
- `focus`(집중 나무): 게임이라기보다 타이머였다
- `roulette`(룰렛 데일리): 운으로만 점수가 정해지고 날짜별 누적 기록이 있어야 성립해
  판마다 초기화하는 방향과 맞지 않았다
- `stock`(모의 주식) · `estate`(부동산 타이쿤): 투기·사행성 소재
- `tuber`(유튜버 시뮬) · `mahjong`(마작 솔리테어): 어린 이용자가 이해하기 어렵거나
  이름이 도박을 연상시킨다
- `reigns`(국가 운영): 지표 관리가 게임이라기보다 통치 시뮬레이션에 가까웠고
  정치·경제 개념 없이는 선택의 의미를 알 수 없었다
- `store`(편의점 운영): 발주·재고·평판을 동시에 굴려야 해 어린 이용자에게 어려웠다

### 점수 단위 기준

게임마다 순위표가 따로 있어 기능상 의미는 없지만, 통일성을 위해 **분당 500~2,000점**
범위에 맞춘다. 새 게임을 만들 때도 이 범위 안에 들어오게 점수 상수를 잡는다.
(2026-07 기준 측정값은 21개 중 20개가 이 범위 안에 있다.)

예외 — **점프 챌린지**는 점수가 곧 도달 높이(m)라 배율을 곱하면 의미가 깨진다.
`447m = 447점`을 유지한다.

### 게임 소재 기준

전 연령이 30초 안에 이해할 수 있어야 한다. 투기·도박을 소재로 하거나 그렇게 보이는
이름은 쓰지 않는다.
이후 추가는 매주 인기 데이터(최근 7일 총 플레이 시간 × √고유 이용자 수)를 보고
잘되는 장르 위주로 선정한다.

## 게임 하나를 "끝냈다"고 할 기준

새 게임을 넣거나 기존 게임을 다듬을 때 아래 여섯을 다 보고 넘어간다.
건너뛸 거면 왜 건너뛰는지 커밋 메시지에 남긴다.

**① 말과 배경이 물건처럼 보이는가** — 단색 `fillRect`/`arc` 하나로 끝나는 요소는
왼쪽 위 광원 기준으로 명암·두께·하이라이트를 준다. 색만으로 종류를 가르지 말고
**실루엣으로도 갈리게** 한다. 작게 그리면 색은 뭉개지고 모양만 남는다.

다만 실루엣은 **물리 몸체를 벗어나지 않는 선까지**다. 수박 합치기의 배가 서양배
모양이라 반지름 r짜리 충돌 원의 64%밖에 못 채웠고, 하필 한가운데 폭이 원의 42%였다.
눈으로는 비어 보이는데 실제로는 밀려나니 어디서 부딪히는지 알 수 없었다. 지금은
원으로 되돌리고 잔점 무늬로 배임을 알린다 (딸기 84%, 사과 99%는 그대로 둔다 —
모자란 곳이 위쪽 어깨라 부딪히는 자리와 크게 어긋나지 않는다).

**② 조작에 손맛이 있는가** — 누르고·맞고·터지고·사라지는 순간마다 눈에 보이는 반응
(파티클, 눌림/늘어남, 화면 흔들림, 점수 팝업)이 있는가. 소리(`sound.ts`)와 진동이
붙어 있는가. 실패했을 때 **왜 실패했는지** 화면이 말하는가.

**③ 처음 잡은 사람이 30초 안에 이해하는가** — 조작 안내가 **글자 없이** 되는가
(움직이는 손가락 표식 등). 게임 안 문구는 13개 언어를 다 채워야 하므로 안 쓸 수
있으면 안 쓰는 게 낫다. `guides.ts`의 설명이 실제 게임과 맞는가.

**④ HUD가 공통 규격을 따르는가** — `drawScorePanel`을 쓰는가(위에서 물체가
내려오거나 판이 고정된 게임은 `compact: true`). 고유 정보는 `icons.ts` 아이콘 + 숫자로.
큰 숫자 자리(`SCORE_PANEL.valueY`)는 건드리지 않는다 — 게임을 옮겨 다닐 때 숫자가
같은 높이에 있어야 흔들려 보이지 않는다.

**⑤ 숫자가 어디서나 같은 값인가** — 러너에서 실제로 터졌다. 최고 기록이 허브에서는
1,103, 게임 안에서는 367로 나왔다. `localStorage`는 이 기기 기록만 쌓는데 서버 값을
되먹이는 곳이 없어서, 이미 1,103점이 있는데도 400점에 '신기록' 배지가 붙었다.
최고 기록은 `syncLocalBests()`가 맞춰주므로 새 경로를 만들면 거기서도 부른다.
점수 환산식을 바꾸면 **이미 쌓인 기록과 단위가 달라진다는 것**을 알고 바꾼다.

**⑥ 두 테마에서 다 보이는가** — 판 바탕은 `games/scene.ts`의 `ground()`로 뒤집고,
말 색은 두 테마 공통으로 둔다. 캔버스 위 글자가 어두운 판에서 묻히지 않는지 본다
(네모로직 힌트 숫자가 대비 1.2로 사실상 안 보였다).

## 보류

실시간 멀티 계열(io 게임, 퀴즈 배틀, 그림 맞추기, 땅따먹기)은 중독성은 높지만
동시접속 확보와 서버 비용 문제로 보류. 트래픽이 충분해진 뒤 재검토한다.
