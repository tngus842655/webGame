node_modules/
dist/
.vite/
*.local
.DS_Store

# 앱인토스 빌드 중간 산출물과 앱 번들
.granite/
*.ait

# 앱인토스 mTLS 인증서 — 서버 환경 변수로만 넣는다. 저장소에 두지 않는다
*.key
*.crt
*.pem
*.p12

# 서명 키 — 유출되면 누구나 이 앱 이름으로 서명한 앱을 만들 수 있다.
# android/.gitignore에도 있지만 그건 android/ 아래에만 걸린다.
# 저장소 어디에 두든 잡히도록 루트에도 적는다.
*.keystore
*.jks
