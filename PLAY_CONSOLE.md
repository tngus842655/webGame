# Play Console 설정 현황

앱 콘텐츠 설문·스토어 등록정보처럼 **Play Console에서 한 번 정하면 계속 따라다니는 값**을 적어 둔다.
빌드 방법은 `ANDROID_RELEASE.md`에 있다.

같은 설문을 다시 채울 일이 생기므로 답만 적지 않고 **그렇게 답한 이유**를 같이 남긴다.

## 앱 기본

| 항목 | 값 |
| --- | --- |
| 앱 이름 | `MiniGame30` |
| 패키지 | `com.minigame30.app` |
| 기본 언어 | 영어(미국) – en-US |
| 앱 or 게임 | 게임 |
| 카테고리 | 캐주얼 |
| 태그 | 두뇌 게임, 매칭, 아케이드, 캐주얼, 퍼즐 |
| 연락처 이메일 | `tngus842655@gmail.com` |
| 웹사이트 | `https://web-game-ecru.vercel.app/` |
| 외부 마케팅 | 사용 (Play 밖에서 구글이 노출시켜 주는 무료 채널이라 끌 이유가 없다) |

카테고리는 하나만 고를 수 있다. 30개가 장르를 넘나들어 특정 장르로 묶이지 않으므로
**캐주얼**로 두고, 세부 장르는 태그가 커버한다.

## 앱 콘텐츠 (2026-07-29 제출)

| 항목 | 답 | 근거 |
| --- | --- | --- |
| 개인정보처리방침 URL | `https://web-game-ecru.vercel.app/privacy` | |
| 계정 삭제 URL | `https://web-game-ecru.vercel.app/account-deletion` | |
| 로그인 세부정보 (제한된 부분) | 아니요 | 로그인 없이 전부 플레이된다. 관리자 화면은 `v-if="isAdmin"`으로 가려져 심사자가 마주칠 화면이 아니다 |
| 콘텐츠 등급 1페이지 6문항 | 전부 아니요 | 채팅·위치 공유 기능이 코드에 없다 |
| 대상 연령 | 13~15 / 16~17 / 18세 이상 | 아래 "13세 미만을 넣으면 안 되는 이유" 참고 |
| 광고 ID 사용 | 아니요 | H5 Games Ads는 웹 광고라 AAID를 쓰지 않는다. **"예"로 답하면 `AD_ID` 권한이 없는 우리 AAB가 통째로 차단된다** |
| 금융 기능 | 앱에서 금융 기능을 제공하지 않음 | 결제·인앱상품 없음. 리워드 광고는 현금 가치가 없어 해당 없음 |
| 건강 앱 | 해당 없음 | |
| 데이터 보안 — 계정 생성 방법 | OAuth만 | SNS에서 이름이 넘어오는 것은 계정 생성 방법이 아니라 수집 데이터다 |
| 데이터 보안 — 수집 항목 | 이름, 이메일 주소, 사용자 ID, 앱 상호작용 | 전부 수집함 / 공유 안 함 / 목적은 앱 기능 |
| 데이터 보안 — 필수 여부 | 사용자 ID·앱 상호작용은 필수, 이름·이메일은 선택 | 이름·이메일은 SNS 연동을 해야만 들어온다. 일부 사용자에게만 수집돼도 신고 대상이고, "선택사항"으로 표시하면 된다 |
| 데이터 보안 — 위치 | 체크 안 함 | `navigator.geolocation` 호출이 없다 |
| 데이터 보안 — 사진 | 체크 안 함 | 개인정보처리방침에 프로필 사진이 적혀 있지만 저장하는 코드·컬럼이 없다 |
| 데이터 보안 — 앱 성능/기기 ID | 체크 안 함 | Sentry·Crashlytics·애널리틱스 SDK가 없다. Play가 자체 수집하는 크래시는 신고 대상에서 제외 |

### 13세 미만을 넣으면 안 되는 이유

넣는 순간 **Play for Families** 정책 대상이 되는데, TWA는 크롬 커스텀 탭으로 임의의 웹을
띄우는 구조라 이 정책과 호환되지 않는다. `bubblewrap init`도 같은 경고를 한다.
리워드 광고 매체가 아동용 승인 SDK가 아닌 점, 소셜 로그인에 부모 동의가 필요한 점,
닉네임이 자유 입력이라 UGC 관리 요건이 걸리는 점도 겹친다.

콘텐츠만 보면 아동도 할 수 있는 게임이지만, 넣으면 반려가 사실상 확정이다.

### Play Games Sidekick은 하지 않는다

"게임에 Play Games Sidekick을 추가하세요" 배너는 권장 사항이지 출시 요건이 아니다.
Play Games Services 위에 얹히는 기능인데 우리는 랭킹을 Supabase로 직접 굴리고 PGS를
쓰지 않는다. TWA 껍데기에는 네이티브 SDK를 넣을 자리도 없다.

## 스토어 등록정보

문구 초안은 `STORE_LISTING.md`에 있다.

| 항목 | 규격 | 파일 |
| --- | --- | --- |
| 앱 아이콘 | 512×512 PNG | `public/icon/icon-eng-512-v1.png` |
| 그래픽 이미지 | 1024×500 PNG/JPG | `public/icon/sum-eng-1024 500.png` |
| 휴대전화 스크린샷 | 최소 2장, 16:9 또는 9:16 | **아직 없음** |

아이콘은 영문판(`icon-eng-*`)을 쓴다. 스토어 기본 언어가 en-US이고 등록정보도 영문이라
표기를 맞췄다. 한글판(`icon-192-v1.png`, `icon-512-v1.png`)은 이전 버전이라
매니페스트에서 참조하지 않는다.

앱 이름은 언어별로 나뉘지 않고 빌드 시점에 하나로 고정된다. 그래서 매니페스트의
`name`/`short_name`도 `MiniGame30`으로 통일했다. 화면 안 제목(`app.title`)은
언어를 따르므로 한국어는 `미니게임30` 그대로 둔다.

## 트랙 — 순서를 지켜야 시간을 아낀다

| 트랙 | 심사 | 12명·14일 인정 | 용도 |
| --- | --- | --- | --- |
| 내부 테스트 | 거의 없음 (수 분) | 안 됨 | 앱이 제대로 도는지 빨리 확인 |
| 비공개 테스트 | 있음 (수 시간~며칠) | **됨** | 여기서 14일 시계가 돈다 |
| 프로덕션 | 있음 | — | 정식 출시 |

빌드가 완벽해질 때까지 기다릴 필요는 없다. **새 빌드를 올려도 14일 카운터는 리셋되지 않는다.**
시계는 특정 빌드가 아니라 테스터 12명이 옵트인을 유지하는 연속 일수를 센다.

## 12명 / 14일

2023년 11월 13일 이후에 만든 **개인** 개발자 계정은 정식 출시 전에
**서로 다른 구글 계정 12개가 14일 연속으로 비공개 테스트에 참여**해야 한다. (사업자 계정은 면제)

- 각자 옵트인 링크로 참여 + 실기기에 설치해야 인정된다 (에뮬레이터·중복 계정 불가)
- 카운트는 심사 승인 + 12명 옵트인이 모두 충족된 시점부터 시작

리셋의 원인은 빌드가 아니라 **테스터 구성 변화**다. 인원이 12명 아래로 떨어지거나
중간에 새로 추가하면 사이클이 다시 시작될 수 있다. **한 명씩 늘려가는 게 제일 위험하다.**
12명을 다 모을 때까지 기다렸다 한 번에 등록하고, 이탈을 감안해 14~15명을 확보해 둔다.

14일을 채우면 대시보드에 **프로덕션 액세스 신청** 버튼이 열린다.

## 진행 상황

### 2026-07-29 — 비공개 테스트 제출

`비공개 테스트 – Alpha` 트랙에 `1 (1)`을 제출하고 검토 대기 중.

- 176개 국가/지역 추가, 프로덕션과 동기화 해제
- 테스터: 구글 그룹 `tester-match@googlegroups.com`
- 관리형 게시가 꺼져 있어 승인되면 자동 반영된다
- 앱 콘텐츠 11개 항목 전부 완료

이 빌드에는 **영문 아이콘과 `MiniGame30` 앱 이름이 아직 반영되지 않았다.**
매니페스트 값이라 웹 배포 후 TWA를 다시 빌드해야 바뀐다. 다음 라운드 순서:

1. main 머지 → Vercel 배포
2. 배포된 매니페스트에서 `MiniGame30`, `icon-eng-*` 확인
3. `twa-manifest.json`의 `appVersionCode`를 2로
4. `bubblewrap update && bubblewrap build`
5. 같은 트랙에 새 AAB 업로드 (테스터 구성은 건드리지 않는다)

### 남은 일

- 스크린샷 촬영 (최소 2장)
- 테스터 12명 확보
- 배포 환경의 `VITE_ADSENSE_CLIENT` 설정 여부 확인 — 값이 있으면 실제 광고가 나가므로
  개인정보처리방침에 광고 파트너 문단을 추가해야 한다
