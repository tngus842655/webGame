# 게임별 완성도 작업 계획

> 게임 하나씩 시간을 들여 끝까지 다듬는 작업의 기준과 순서.
> 라인업·기획은 [GAME_CONCEPTS.md](./GAME_CONCEPTS.md), 구조는 [DESIGN.md](./DESIGN.md).

## 0. 지금까지 무엇이 끝났나

허브·공통 UI는 한 바퀴 돌았다. 아래는 **이미 갖춰진 도구**이므로 게임 작업 때
새로 만들지 말고 가져다 쓸 것.

| 도구 | 위치 | 쓰는 곳 |
|---|---|---|
| 캔버스 글씨체 | `games/ui.ts` `font(size, bold)` | 캔버스 텍스트 전부 |
| 게임 전용 아트 | `games/fruitArt.ts` · `deck/cardArt.ts` · `autochess/unitArt.ts` | 그 게임에서만 쓰는 그림 |
| 상단 점수판 | `games/ui.ts` `drawScorePanel` / `SCORE_PANEL` | 게임 HUD |
| 벡터 아이콘 15종 | `games/icons.ts` `drawIcon` / `drawIconValue` / `drawIconRow` | HUD의 하트·코인·타이머 등 |
| 게임오버·클리어 팝업 | `games/overlay.ts` / `clearBonus.ts` | 전 게임 공통 |
| 효과음 21종 | `shared/sound.ts` `playSfx` / `preloadSfx` | 전 게임 공통 |
| 버튼 | `styles/main.css` `.btn` `--go` `--amber` `--ghost` `--sm` | 팝업·허브 공통 |
| 화면 조작 아이콘 | `shared/UiIcon.vue` | 허브 화면 |

**캔버스에 이모지를 글자로 찍지 말 것.** 기기마다 그림이 다르고 숫자와 높이가
어긋난다. 필요한 그림이 `icons.ts`에 없으면 거기에 추가한다.

## 1. 게임 하나를 "끝냈다"고 할 수 있는 기준

한 게임을 잡으면 아래 다섯 가지를 모두 보고 나서 다음으로 넘어간다.
하나라도 건너뛸 거면 왜 건너뛰는지 커밋 메시지에 남긴다.

### ① 말과 배경이 물건처럼 보이는가

- 단색 `fillRect` / `arc` 하나로 끝나는 요소가 있는가 → 광원(왼쪽 위) 기준
  명암, 두께, 하이라이트를 준다.
- 색만으로 종류를 가르고 있는가 → **실루엣으로도 갈리게** 한다.
  색맹 여부를 떠나, 작게 그리면 색은 뭉개지고 모양만 남는다.
  (참고: `fruitArt.ts`는 딸기=원뿔·사과=꼭지 홈·배=조롱박으로 갈라 둔다.)
- 배경이 한 겹인가 → 시차(parallax)를 줄 수 있는 게임이면 층을 나눈다.

### ② 조작에 손맛이 있는가

- 누르고·맞고·터지고·사라지는 순간마다 **눈에 보이는 반응**이 있는가.
  파티클, 눌림/늘어남(squash·stretch), 화면 흔들림, 점수 팝업.
- 소리(`sound.ts`)와 진동(`vibrate`)이 붙어 있는가.
- 실패했을 때 **왜 실패했는지** 화면이 말해주는가.

### ③ 처음 잡은 사람이 30초 안에 이해하는가

- 조작 안내가 **글자 없이** 되는가 (움직이는 손가락 표식 등).
  게임 안 문구는 13개 언어를 다 채워야 하므로, 안 쓸 수 있으면 안 쓰는 게 낫다.
- 지금 무엇을 노려야 하는지(목표·남은 기회·제한 시간)가 한눈에 보이는가.
- `guides.ts`의 설명이 실제 게임과 맞는가.

### ④ HUD가 공통 규격을 따르는가

- `drawScorePanel`을 쓰는가. 위에서 물체가 내려오거나 판이 고정된 게임은
  `compact: true`.
- 게임 고유 정보(목숨·골드·시간)는 `icons.ts`의 아이콘 + 숫자로.
- 큰 숫자 자리(`SCORE_PANEL.valueY`)는 건드리지 않는다 — 게임을 옮겨 다닐 때
  숫자가 같은 높이에 있어야 흔들려 보이지 않는다.

### ⑤ 숫자가 어디서나 같은 값인가

**러너에서 실제로 터진 문제다.** 최고 기록이 허브에서는 1,103, 게임 안에서는
367로 나왔다. `localStorage`는 이 기기 기록만 쌓는데 서버 값을 되먹이는 곳이
없었고, 그 탓에 이미 1,103점이 있는데도 400점에 '신기록' 배지가 붙었다.

- 같은 사실을 두 곳(로컬·서버)에서 읽고 있지 않은지 본다.
- 최고 기록은 `syncLocalBests()`가 맞춰준다. 새 경로를 만들면 거기서도 부른다.
- 점수 환산식(`scoreOf`)을 바꾸면 **이미 서버에 쌓인 기록과 단위가 달라진다.**
  바꿀 거면 그 사실을 알고 바꾼다.

## 2. 작업 순서

### A. 물건이 물건처럼 안 보이던 게임 — 한 바퀴 끝

| 게임 | 한 일 |
|---|---|
| 리듬 탭 | 노트 명암·레인 표식, 판정 연출, 곡 진행 막대. **완주해도 결과 팝업이 안 뜨던 버그** |
| 궤도 슈팅 | 적을 쐐기/방패 육각으로 갈라 그림, 포신 반동, 격추 점수 팝업 |
| 카드 배틀 | `cardArt.ts` 8종, 적을 덩치로, 피해 숫자, 적 의도를 머리 위로 |
| 오토 배틀 | `unitArt.ts` 4종, 전투 모션·투사체, **승리 배너가 실제 점수와 달랐던 것** |
| 분류 반장 | 도형에 두께, 게이트를 통으로, 놓친 도형이 부서짐 |
| 종이배 뱃길 | 물·항적·부두, 색맹 대비 표식, 충돌 경고선 |
| 별똥별 잇기 | 이은 자리를 별자리로 묶는 선, 놓친 자리 표시 |
| 펭귄 미끄럼 | 얼음 가루·미끄럼 자국·부딪힘, 얼음판 질감 |
| 대나무 타기 | 숲 3겹 시차, 건너가는 동작, 마디 있는 줄기 |

### B. 판·글자 중심 게임 — 한 바퀴 끝

여기서 본 것은 **맞았을 때·틀렸을 때의 반응**, 글자 대비, 손가락 과녁이다.

| 게임 | 한 일 |
|---|---|
| 숫자 줄 세우기 | 칸 과녁 확대, 카드가 칸으로 날아감, 거부 흔들림 |
| 찰나의 숫자 | 점수판 통일, 오답 화면 반응, 다음 자리 맥동 |
| 같은 그림 찾기 | 점수판 통일, 정답 확대·점수 팝업, 첫 판 규칙 안내 |
| 오목 | **눌러서 미리 보고 떼면 착수** (교차점이 손끝의 절반이었다) |
| 한글 워들 | **줄이 뒤집히며 결과가 열림**, 낼 수 없는 줄 흔들림, 점수판 통일 |
| 스도쿠 · 네모로직 · 파이프 연결 | 열어 보고 그대로 뒀다 — 볼 것이 이미 갖춰져 있었다 |

### C. 그 전에 손댄 것

| 게임 | 한 일 |
|---|---|
| 무한 러너 | 캐릭터·배경 3층·먼지·코인 연출·점수판 (전면) |
| 타워 디펜스 | 포탑 형태, 포신이 적을 조준 |
| 탑 쌓기 | 블록 두께·명암 |
| 구슬 정리함 | 유리구슬 |
| 낙하물 회피 · 서바이버 | 점수판 통일 |
| 수박 · 블록블라스트 · 벽돌깨기 · 과일2048 · 머지 가든 · 구슬 정리함 | 조각 아트는 원래 갖춰져 있었고, 점수판만 공통 규격으로 맞췄다 |
| 매치3 · 트라이픽스 | 이미 공통 점수판을 쓰고 있다 |

### D. 조작·정보 보완 — 헤드리스로 31판을 돌려 보고 나온 것들

공통:

| 무엇 | 어디 |
|---|---|
| 일시정지 (버튼·시트·3-2-1 복귀, 탭 전환 시 자동) | `pages/GamePlayPage.vue` |
| 게임오버 사유 한 줄 (시간·목숨·체력·이동·막힘·클리어) | `games/overlay.ts` `show(…, reasonKey)` |
| 결과 팝업 부가 기록 칸 | `games/overlay.ts` `show(…, stats)` |

게임별:

| 게임 | 한 일 |
|---|---|
| 스도쿠 | 메모(펜슬 마크) — 3×3 후보 표기, 숫자를 놓으면 같은 줄·박스 메모 자동 삭제 |
| 카드 배틀 | 덱 더미를 눌러 보유 카드 전체를 종류별 장수로 확인 |
| 오토 배틀 | 준비 단계에서 이번 라운드 적 부대를 미리 보여 준다 |
| 리듬 탭 | 노트 속도 ×0.8~×1.6 (localStorage 유지), 결과에 판정 분포 |
| 오목 | 도달한 단·그 시점 점수를 저장해 이어하기 (점수는 부풀지 않는다) |
| 펭귄 미끄럼 | 판 다시하기 (되돌리기 몫 1회 소모) |
| 종이배 | 톡 짚고 떼면 항로 지우기 / 짚기만 해서는 지워지지 않게 |
| 서바이버 | 레벨업 화면에 지금까지 고른 강화와 횟수 |
| 같은 그림 찾기 | 마지막 남은 게임 내 문구 제거 — 정답 두 개를 잇는 점선으로 대체 |
| 네모로직 | 짚은 칸의 줄 띠·테두리·해당 힌트 숫자 강조 (10×10에서 손가락이 칸을 가렸다) |

### E. 첫 화면 로딩

메인 청크 641KB(gzip 201KB) → **309KB(gzip 110KB)**.

| 무엇 | 어떻게 |
|---|---|
| 번역 사전 (gzip 40KB) | 언어별 모듈(`shared/locales/*`)로 나눠 고른 언어 하나만 받는다 |
| supabase-js (gzip 56KB) | `getSupabase()`로 처음 쓰는 순간에 받는다 — 홈 기록은 로컬에서 먼저 읽는다 |

`shared/i18n.ts`에는 en 폴백과 타입(`FullDict`·`CoreDict`)만 남는다.
언어 하나를 추가하려면 `locales/<code>.ts`를 만들고 `LOADERS`에 한 줄 등록한다.

## 2-1. 다음에 볼 것

한 바퀴는 돌았으니, 다음은 **직접 해보고 걸리는 지점을 적는 것**부터다.
아래는 이번에 손대면서 남겨 둔 것들.

- 소리는 `sound.ts`에 21종이 있고 `public/sfx/*.wav`로 만들어 두었다.
  더 나은 소리를 구하면 같은 이름의 `.mp3`를 같은 폴더에 넣으면 그쪽이 이긴다
  (`SOUND_REQUEST.md`에 소리별 느낌과 출처 후보를 적어 뒀다).
- 메인 청크에 아직 gzip 110KB가 남아 있다. 절반은 Vue·라우터(36KB)라
  더 줄이려면 페이지 단위 지연 로딩이 필요하다 — 체감이 걸릴 때 본다.

## 3. 한 게임을 잡았을 때의 진행

```
1. 게임을 직접 해본다 → 걸리는 지점을 먼저 적는다
2. index.ts / renderer.ts / state.ts를 읽는다
3. 1장의 다섯 기준으로 고칠 목록을 만든다 → 사용자에게 보여주고 시작
4. 고친다 (아트 → 연출 → HUD 순서. 뒤로 갈수록 앞의 결정에 기댄다)
5. npm run typecheck && npm run build
6. 커밋 (한글, 무엇을 왜 바꿨는지)
7. 사용자가 직접 해보고 확인 → 다음 게임
```

**스크린샷으로 확인하지 않는다** (CLAUDE.md). 타입 체크와 코드 리뷰로 검증하고
실제 확인은 사용자가 한다. 그래서 3번에서 목록을 먼저 보여주는 것이 중요하다.

## 4. 손대지 말아야 할 것

- **잘 그려진 아트를 취향으로 다시 그리지 않는다.** 수박의 과일, 낙하물의
  장애물, 서바이버의 업그레이드 카드는 이미 완성돼 있다.
- **게임 규칙·난이도를 아트 작업 중에 바꾸지 않는다.** 숫자를 만지면 이미
  쌓인 랭킹과 어긋난다. 밸런스는 따로 잡는다.
- **공통 규격을 게임 하나 때문에 넓히지 않는다.** `drawScorePanel`에 옵션을
  더하기 전에, 그 게임만의 사정인지 여러 게임에 걸친 사정인지 본다.
  (compact는 낙하물·서바이버 둘 다 필요해서 넣었다.)
- 원래부터 있던 죽은 코드는 요청 없이 지우지 않는다.
