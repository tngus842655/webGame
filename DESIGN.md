# 미니게임30 기술 설계

> 게임 라인업·향후 계획은 [GAME_CONCEPTS.md](./GAME_CONCEPTS.md) 참고.
> 이 문서는 현재 구조의 참조용 요약이다. DB 스키마의 원본은 `supabase/migrations/`.

## 1. 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 프레임워크 | Vue 3 + Vite + TypeScript | 허브 UI 전담 |
| 게임 렌더링 | Canvas 2D 직접 렌더링 | 외부 이미지 에셋 0개 유지 (벡터·이모지 드로잉) |
| 물리 엔진 | matter.js | 수박게임에만 사용, 게임 모듈 안에 격리 |
| 라우팅 | vue-router | 게임 = `/play/:slug` 라우트 하나 |
| 백엔드 | Supabase (Auth + Postgres + RLS) | 점수·랭킹·프로필·플레이 시간 |
| i18n | 자체 경량 구현 (`shared/i18n.ts`) | 13개 언어, 게임 내부는 ko/en 완역 + en 폴백 |
| 사운드 | WebAudio 신스 (`shared/sound.ts`) | 오디오 에셋 0개 |
| 운영 배포 | Vercel (`main`) | main 반영은 사용자가 직접 수행 |
| 앱 패키징 | Capacitor (안드로이드) / 앱인토스 | 둘 다 웹 빌드를 앱 안에 넣는다 (`ANDROID_RELEASE.md`) |
| 타입 검증 | `npm run typecheck` (vue-tsc) | 커밋 전 통과 필수 |

### 환경 변수 (`.env.local`, 배포 대시보드)

```
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
```

## 2. 아키텍처 원칙

1. **게임은 허브를 모른다.** 게임 모듈은 `GameContext`(점수 제출·광고)만 주입받는다.
2. **허브는 게임 내부를 모른다.** 동적 import로 로드해 mount/unmount할 뿐이며,
   게임별 라이브러리는 해당 게임 청크에만 번들된다.
3. **게임 추가 = 폴더 하나 + 레지스트리 한 줄.**

## 3. 프로젝트 구조

```
src/
├─ main.ts                  # 앱 부트스트랩 + 익명 세션 시작
├─ app/                     # router, AppLayout
├─ pages/                   # Home / GamePlay / RankingHub / Ranking / Stats / Settings
│                           #   / DevNotes / Terms / Privacy / AccountDeletion / Trash
├─ games/
│  ├─ types.ts              # GameMeta / GameModule / GameContext
│  ├─ registry.ts           # 게임 목록 (여기 한 줄 추가로 게임 등록)
│  ├─ shell.ts              # 공통 골격: rAF 루프, 백그라운드 일시정지, 정리
│  ├─ stage.ts / overlay.ts / clearBonus.ts / pointer.ts   # 스테이지·게임오버·클리어 보너스·입력
│  ├─ scene.ts              # ground(라이트, 다크) — 게임 판 바탕만 테마를 탄다
│  └─ <slug>/               # 게임 모듈 (index.ts 진입점, state/renderer/config…)
├─ shared/
│  ├─ supabase.ts           # 클라이언트 싱글턴
│  ├─ auth.ts               # 익명 세션, SNS 연동(link)·로그인·계정 전환
│  ├─ OnboardingFlow.vue   # 첫 실행 온보딩 (언어 → 기존 회원 → 닉네임)
│  ├─ profile.ts            # 닉네임 조회·수정, 소셜 닉네임 자동 적용
│  ├─ scores.ts             # 점수 제출·랭킹·인기도 조회 (+localStorage 병행)
│  ├─ playSessions.ts       # 플레이 시간 기록 (인기 순위·통계 근거)
│  ├─ ads.ts                # 리워드 광고 추상화 (H5 Games Ads / AdMob / 개발용 스텁)
│  ├─ native.ts             # 안드로이드 앱(Capacitor) 전용 — 딥링크 로그인, 뒤로가기, 스플래시
│  ├─ music.ts             # BGM 재생 (public/bgm/*.mp3, 파일 없으면 무음)
│  ├─ theme.ts              # 라이트·다크·시스템. <html data-theme> + isDarkTheme()
│  ├─ library.ts            # 즐겨찾기·최근 플레이 (localStorage 전용)
│  └─ i18n.ts / sound.ts / GameIcon.vue / GameCard.vue / FavoriteButton.vue / devNotes.ts
└─ styles/
```

## 4. 게임 플러그인 규격

원본은 `src/games/types.ts`. 요약:

- `GameMeta` — `slug`(라우트·DB `game_slug`와 동일), `titleKey`(i18n), `loader`(동적 import)
- `GameModule` — `mount(host, ctx)` / `unmount()`. 리소스 정리는 게임 책임이며,
  `createGameShell()`(shell.ts)을 쓰면 rAF 루프·백그라운드 일시정지·정리가 해결된다.
- `GameContext` — `submitScore`, `getBestScore`, `isRewardAdReady`, `showRewardAd`

### 새 게임 추가 체크리스트

1. `src/games/<slug>/` 폴더에 `GameModule` 구현 (`shell.ts`·`stage.ts` 활용)
2. `registry.ts`에 항목 추가
3. `i18n.ts`에 `game.<slug>` 제목 키를 13개 언어로 추가
4. `GameIcon.vue`에 slug별 벡터 아이콘 추가
5. `npm run typecheck` 통과 확인

## 5. 데이터·인증

스키마 원본: `supabase/migrations/` (신규 환경은 순서대로 실행)

| 테이블 | 내용 |
|---|---|
| `profiles` | auth.users 1:1. `nickname`, `nickname_set`(직접 설정 여부 — 소셜 닉네임 자동 적용 판단) |
| `scores` | append-only 플레이 기록. 유저별 최고점은 `get_leaderboard`가 집계 |
| `play_sessions` | 게임별 플레이 시간. 인기 순위(`get_game_popularity`)·통계의 근거 |
| `admin_emails` | 관리자 이메일 목록. RLS를 켜고 정책을 두지 않아 클라이언트에서 읽지도 쓰지도 못한다 |
| `game_flags` | 게임별 노출 설정(`featured`/`hidden`/`sort_order`). 조회 공개, 쓰기는 관리자만 |

### 인증 흐름 (구현 완료)

- 첫 방문 시 익명 로그인 자동 수행 → 가입 없이 점수 저장·랭킹 등재
- 설정에서 구글/카카오 연동(`linkIdentity`) → user_id 유지, 기록 그대로
- 이미 쓰이는 소셜 계정이면 로그인(`signInWithOAuth`)으로 전환해 기존 기록 복구
- 닉네임을 직접 정하지 않은 유저는 연동 시 소셜 닉네임 자동 적용
- 필요한 외부 설정: 구글/카카오 콘솔 + Supabase(Manual Linking ON,
  Kakao는 Allow users without an email ON) — 상세는 마이그레이션 파일 주석 참고

### 소리

- `sound.ts` — `public/sfx/<이름>.mp3`를 먼저 찾고 없으면 `.wav`, 그것도 없으면
  WebAudio 신스로 떨어진다. 지금은 **wav 21개**가 들어 있다 — 녹음이 아니라
  `tools/gen-sfx.py`가 파형을 합성해 구운 것이라 그림과 같은 결을 갖는다.
  같은 이름의 mp3를 넣으면 그쪽이 이긴다. 소리 크기는 `sound.ts`의 `gain`으로 맞춘다.
- 같은 소리가 20ms 안에 겹치면 건너뛴다 (한 프레임에 여러 번 울리면 뭉쳐서 커진다).
  연쇄는 파일을 늘리지 않고 음을 올려 표현한다.
- `music.ts` — BGM. `public/bgm/`에 `action.mp3` / `puzzle.mp3` / `sim.mp3` 세 개를 넣으면
  `bgmFor(slug)`가 게임별로 골라 반복 재생한다. 파일이 없으면 조용히 넘어가므로
  음원 없이도 앱은 그대로 돈다. 효과음과 별도 토글(설정 화면)을 둔다.
- 음원 라이선스는 상업적 이용 가능 + 광고 게재 가능한 것만 쓴다(NC 조건 금지).
  저작자 표시가 필요한 곡을 쓰면 개발노트 화면에 크레딧을 남긴다.

### 리워드 광고

- `ads.ts`가 매체를 감싼다. 웹·앱인토스는 `VITE_ADSENSE_CLIENT`가 있으면 H5 Games
  Ads(AdSense Ad Placement API), 안드로이드 앱은 `VITE_ADMOB_REWARD_ID`가 있으면
  AdMob, 둘 다 없으면 5초 카운트다운 가짜 광고를 쓴다 — 실제 매체를 붙이기
  전에도 배포본에서 버튼·보상 흐름을 그대로 확인하기 위해서다.
- 앱에서 매체가 갈리는 이유는 정책이다. AdSense는 웹사이트용 상품이라 앱 웹뷰 안에서
  게재하면 프로그램 정책 위반이고, 앱에서 쓸 수 있는 구글 매체는 AdMob 쪽이다.
- 노출 지점은 두 가지. **실패 쪽**은 게임오버 오버레이의 이어하기(대부분의 게임),
  **성공 쪽**은 `clearBonus.ts`의 점수 2배(스도쿠·네모로직·디펜스·머지 가든).
  한 판이 10분 넘게 가는 게임은 실패가 잘 나지 않아 성공 쪽이 유일한 접점이다.
- 리워드 광고에는 횟수 상한을 두지 않는다. 사용자가 버튼을 눌러야 나오는 광고라
  강제 노출과 달리 이탈을 만들지 않고, 빈도 조절은 광고 매체가 알아서 한다.
  강제로 뜨는 전면 광고를 넣게 되면 그때 그쪽에 빈도 제한을 건다.
- **보상 지급 규칙**: `show()`는 "보상을 줄지"를 돌려준다. 끝까지 봤으면 당연히 주고,
  **재고 없음·빈도 상한·에러처럼 매체 사정으로 광고가 안 나온 경우에도 준다.**
  버튼을 눌렀는데 광고가 없어서 못 본 것은 사용자 잘못이 아니다.
  보상을 막는 건 사용자가 스스로 광고를 닫았을 때(`dismissed`)뿐이다.

### 첫 실행 온보딩

계정은 **온보딩을 마쳐야 만들어진다.** 예전에는 앱이 뜨자마자 익명 로그인을 해서
사이트만 열고 나간 방문자·크롤러까지 계정이 쌓였고, 닉네임도 `게스트-3f2a` 같은
무작위 값이 랭킹에 그대로 나갔다.

순서는 언어 → 기존 회원 유무 → 닉네임이다.
- **언어**: 브라우저 감지값을 기본으로 두고 고르게 한다 (고르는 즉시 화면에 반영)
- **기존 회원**: 여기서 소셜 로그인하면 익명 계정을 만들지 않고 기존 계정으로 바로 붙는다
- **닉네임**: 2~12자. 이 단계를 마치는 시점에 `ensureUserId()` → 계정 생성 → 닉네임 저장

`AppLayout.vue`가 `webgame:onboarded` 플래그로 게이트한다. 플래그가 없어도 세션이
살아 있으면(소셜 로그인 복귀·저장소만 지워진 재방문) 다시 묻지 않는다.

### 홈 화면 구성

위에서부터 인기 TOP3 → 최근 플레이 → 탭 → 휴지통 입구 순이다.

| 자리 | 담는 것 |
|---|---|
| 인기 TOP3 | 신규를 뺀 나머지 중 인기 1~3위. 금빛 바탕을 깐 칸에 카드 셋 |
| 최근 플레이 | 최근 연 게임 최대 8개, 가로 한 줄. 없으면 칸째 그리지 않는다 |
| 탭 · 신규 게임 | 관리자가 올린 게임 (최대 3개, `sort_order` 순) |
| 탭 · 즐겨찾기 | 카드 오른쪽 위 별로 담은 게임 |
| 탭 · 더 많은 게임 | 그 아래 전부. 좌우로 흐르는 카드 |

기본 탭은 '더 많은 게임'이다 — 셋 중 유일하게 항상 차 있다. 고른 탭과 캐러셀이
멈춘 자리는 컴포넌트 밖(모듈 스코프)에 남겨, 게임을 다녀와도 보던 자리로 돌아온다.
앱을 껐다 켜면 기본값으로 돌아간다.

**순위 숫자는 카드에 붙이지 않는다.** 스물몇 장에 번호가 흩어지면 정작 상위 셋이
눈에 안 들어온다. 1~3위는 카드 자체가 금·은·동 테를 띠고, 1위만 테를 굵혀 한 칸
앞으로 나온다. 테는 `outline`으로 준다 — `box-shadow`로 두면 누를 때 그림자를
바꾸면서 테까지 사라진다.

**캐러셀은 끝이 없다.** 같은 목록을 세 벌 이어 붙이고 가운데 벌에서만 논다. 스크롤이
멎으면(140ms) 한 벌 폭만큼 소리 없이 되돌린다 — 되돌리는 거리가 칸 간격의 정확한
배수라 화면에 보이는 것은 그대로다. 되돌리기를 스크롤 도중에 하면 튕긴 힘이 그
자리에서 죽으므로 **반드시 멎은 뒤에** 한다.

가운데 카드가 120이면 양옆은 60이다. 이 비율은 **키우는 쪽이 아니라 줄이는 쪽으로**
잡는다. 카드를 확대하면 글자가 흐려진다.

**첫 실행에는 서버 응답을 기다린다.** 평소 홈은 노출 설정·인기도를 캐시에서 읽고
서버 응답은 캐시만 갱신해 다음 진입에 반영한다(보는 도중 카드가 움직이지 않게).
그런데 설치 직후에는 그 캐시가 없어 '신규 게임' 칸이 빈 채로 떴다. `hasCachedFlags()`가
거짓일 때만 응답을 기다렸다 목록을 세운다.

즐겨찾기와 최근 플레이는 `shared/library.ts`가 localStorage에 담는다. 계정 없이도
바로 눌러야 하고 잃어도 다시 누르면 그만이라 점수처럼 서버까지 태우지 않는다.
기기를 옮기면 따라오지 않는다는 뜻이므로, 서버로 옮길 거면 `user_favorites` 표와
RLS가 필요하다.

카드는 `shared/GameCard.vue` 하나를 홈과 휴지통이 나눠 쓴다. 별은 홈에서만 붙인다 —
휴지통 게임은 즐겨찾기 칸(주 목록 기준)에 올라올 수 없어 눌러도 갈 곳이 없다.

`popularityRanks(games)`에는 **반드시 현재 레지스트리의 게임만 넘긴다**(지금은 관리자
화면만 쓴다). 인기도 캐시는 `play_sessions`를 집계하는데 이미 지운 게임의 기록도 그
표에 남아 있어서, 그것까지 세면 화면에 없는 게임이 번호를 차지한다.

### 테마

라이트·다크·시스템 셋 중에 고른다(`shared/theme.ts`, 설정 화면). 색은 `styles/main.css`에
사용자 지정 속성 17개로 모여 있고 `[data-theme='dark']`에서 다시 정의한다. 허브 화면
열일곱 곳이 하드코딩된 색 대신 이 토큰을 본다.

첫 적용은 `index.html`의 인라인 스크립트가 한다 — 번들은 defer라 그때까지 못 오고,
다크로 들어갈 때 밝은 화면이 한 번 스치면 그게 제일 먼저 보이는 인상이 된다.
저장소 키(`webgame:theme`)를 바꾸면 두 곳을 같이 고쳐야 한다.

**캔버스는 CSS 변수를 못 읽는다.** 게임은 `isDarkTheme()`를 매 프레임 보고 판 색을
고른다(`games/scene.ts`의 `ground(라이트, 다크)`). 바꾸는 것은 판 바탕·여백·그릇·
캔버스 위 글자뿐이고, **말(과일·블록·보석)은 두 테마 공통**이다 — 어둡게 바꾸면
다른 게임이 된다. 원래부터 어두운 판을 쓰는 게임 열다섯 개는 부르지 않는다.

퍼즐 격자(네모로직·파이프·스도쿠·워들)는 흰 판 그대로 뒀다. 숫자·메모·강조색이
흰 판을 전제로 짜여 있어 판만 뒤집으면 읽히지 않는다.

### 다국어

사용자에게 보이는 문구는 13개 언어를 전부 채운다 — 허브 화면, 게임 내부 HUD,
게임 설명 가이드(`guides.ts`), 광고·이어하기 버튼까지. 예외는 둘이다.

- **관리자·통계·개발노트** — 운영자만 보므로 ko/en만 두고 나머지는 en 폴백
- **개인정보처리방침** — 법적 문서라 임의 번역 시 효력 문제가 생길 수 있어 한국어만

`t()`의 키 타입은 `keyof typeof en`이라 없는 키를 쓰면 `vue-tsc`가 잡는다. 다만
**자리표시자(`{n}`) 누락은 타입으로 못 잡는다** — 번역문에서 빠지면 숫자가 통째로
사라지므로 언어를 추가할 때 en과 대조해 확인할 것.

### 관리자

관리자 여부는 **이메일**로 정한다. `admin_emails`에 이메일을 넣는 일은 Supabase
대시보드에서 직접 한다 — 앱에서 관리자를 추가하는 화면은 만들지 않는다.
익명 계정에는 이메일이 없으므로 관리자는 반드시 구글/카카오로 로그인해야 한다.

- `is_admin()` — `security definer`. `auth.jwt() ->> 'email'`을 `admin_emails`와 대조한다.
- **화면을 숨기는 것은 안내일 뿐**이다. 실제 차단은 두 군데서 한다:
  `game_flags` 쓰기는 RLS 정책이, `get_game_stats()`는 함수 첫 줄의 관리자 검사가 막는다.
- 관리자 화면(`/admin`)에서 하는 일:
  - **신규**(`featured`) — 홈 맨 위 '신규' 칸에 올린다. 신규 게임은 기록이 없어
    인기순으로는 맨 뒤로 밀리기 때문이다. 칸이 카드 한 줄이라 `FEATURED_MAX`(3)까지만
    켤 수 있다. 그 안의 자리(`sort_order`)는 **체크한 차례대로** 1·2·3이 매겨지고,
    중간 것을 해제하면 뒤가 당겨진다 — 관리자가 숫자를 직접 넣지 않는다.
    휴지통에 넣으면 고정도 함께 풀린다 — 되돌렸을 때 칸이 넘치지 않도록.
  - **휴지통**(`trashed_at`)·**숨김**(`hidden`) — 아래 참고.
- 통계(`/stats`)가 세는 것: 기간은 **롤링**이다(`now() - n일`, 달력 기준 아님).
  '플레이 구간'은 판 수가 아니라 끊기지 않고 이어서 논 구간의 수다 —
  `startPlayTracking`이 백그라운드 전환 시점에 기록을 끊기 때문이다. 방치 시간이
  섞이지 않는 대신 판 수를 잃는데, 인기 순위가 시간 기반이라 시간 정확도를 택했다.
  이용자 수는 게임별 값으로 만들 수 없어(같은 사람이 여러 게임을 하면 겹친다)
  `get_total_players()`로 따로 센다.
- 통계(`/stats`)는 관리자만 볼 수 있다. 홈의 📊 링크는 🛠️ 관리자 링크로 바뀌었고
  관리자에게만 보인다. `get_game_popularity()`는 홈 정렬에 필요하므로 공개로 남긴다.
- 노출 설정은 인기도와 같이 localStorage에 캐시해 첫 렌더부터 쓴다 — 보는 도중
  카드가 움직이지 않도록 새로 받은 값은 **다음 진입부터** 반영한다.

### 휴지통

목표는 30개다. 그 이상으로 늘어나면 인기 상위 30위 밖으로 밀린 게임을 주 목록에서
덜어낸다. 다만 **없애는 게 아니라 옮기는 것**이다 — 휴지통은 사용자에게도 보이는
공간이고, 내려간 게임도 거기서 그대로 즐길 수 있다.

게임이 놓이는 자리는 두 플래그가 정한다.

| | `trashed_at` | `hidden` | 사용자가 보는 곳 |
|---|---|---|---|
| 정상 | null | false | 홈 주 목록·랭킹 |
| 휴지통 | 있음 | false | 홈 하단 휴지통(`/trash`) |
| 숨김 | 있음 | true | 없음 |

- **자동으로 버리지 않는다.** 인기도는 오르내리는 값이라 잠깐 밀린 게임이 자동으로
  내려가면 곤란하고, 최종 삭제는 결국 사람이 하는 커밋이다.
  관리자 화면은 30위 밖 게임의 순위를 주황색으로 **표시만** 하고, 버리는 건 버튼으로 한다.
- **숨김은 휴지통에 있는 게임에만 건다**(`/admin/trash`). 사용자 휴지통에서도 내려야
  할 때 쓴다. 정상 게임을 바로 숨기는 길은 두지 않았다 — 주 목록에서 내리는 것과
  아예 없애는 것은 한 단계씩 밟게 한다.
- `/play/<slug>` 직접 접근은 어느 상태에서도 막지 않는다 — 휴지통 게임은 애초에
  즐기라고 남긴 것이고, 숨김도 북마크로 들어온 사람을 끊을 이유가 없다.
- 보관 기간(`RETENTION_DAYS`, 30일)이 지나면 관리자 화면에 '제거 가능'으로 뜬다.
  그때 하는 일은 **커밋**이다: `src/games/<slug>/` 삭제 → `registry.ts`에서 제거 →
  게임 제목 번역 키 정리. `game_flags` 행은 남겨둬도 무해하다(레지스트리에 없으면
  화면에 안 나온다).
- 되돌리기는 언제든 된다 — 코드를 아직 안 지웠으면 그대로 살아난다.
- 사용자 화면의 이름은 `trash.title` 한 줄이다. '지난 게임'·'보관함'처럼 바꾸고 싶으면
  거기만 고치면 된다.

### RLS·치팅 대비

- `scores` insert는 본인만(`auth.uid() = user_id`), update/delete 금지
- 점수 상한 check(0~1,000,000), 동일 유저 분당 6회 제출 제한
- 클라이언트 게임 특성상 완전한 검증은 불가 — 서버 검증은 트래픽 생긴 뒤 도입

## 6. 확장 지점 (미구현)

- **재화·상점**: 광고와 세트로 도입 시점에 설계
